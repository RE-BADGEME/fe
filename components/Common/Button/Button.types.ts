import { ReactNode } from 'react';

export type RoundRoutingButtonProps = {
  children: ReactNode;
  routingPath?: string;
  color?: string;
};

export type SimpleButtonProps = {
  children: ReactNode;
  color?: string;
};
